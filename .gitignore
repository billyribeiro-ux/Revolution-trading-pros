# Node modules
node_modules/
**/node_modules/

# Build outputs
frontend/build/
frontend/.svelte-kit/
frontend/dist/

# Environment files
.env
.env.local
.env.*.local
frontend/.env.e2e
!frontend/.env.production

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Database
*.sqlite
*.sqlite-journal

# Package locks (optional - uncomment if you want to ignore)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Temporary files
*.tmp
*.temp
*.zip
modified_subscription_files.zip

# ═══════════════════════════════════════════════════════════════════════════════
# SECURITY - Critical files that must NEVER be committed
# ═══════════════════════════════════════════════════════════════════════════════

# Credential and secret files
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
credentials.json
service-account*.json
secrets.yaml
secrets.yml
.secrets
.credentials

# SSH keys
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.ppk

# API keys and tokens
.api_keys
api_keys.txt
tokens.json
oauth*.json

# Backup files (may contain sensitive data)
*.bak
*.backup
*.sql
*.dump
*_backup*
*_bak*

# Exception: Allow migration files
!api/migrations/*.sql

# AWS/Cloud credentials
.aws/
.azure/
.gcloud/
google-cloud-key*.json

# Docker secrets
docker-compose.override.yml
.docker-secrets/

# Terraform state (may contain secrets)
*.tfstate
*.tfstate.*
.terraform/

# PHPUnit coverage reports (may expose code paths)
coverage/
.phpunit.result.cache

# Playwright test results
frontend/test-results/
frontend/playwright-report/
frontend/playwright/.cache/

# E2E test environment and auth state
frontend/.env.e2e
frontend/tests/e2e/.auth/*.json

# Test configuration with real credentials
phpunit.xml
.phpunit.xml

# IDE workspace settings (may contain paths/credentials)
*.code-workspace
api-rust/target/

# Backend vendor (Laravel dependencies - REMOVED)
backend/vendor/

# Retired/archived code and build artifacts
_retired/

# Rust build artifacts
backend-rust/target/
api/target/
**/target/
