# Windsurf AI Rules - Revolution Trading Pros v2
## Apple Principal Engineer ICT Level 7 | January 2026

# 🚨 ZERO ERRORS TOLERANCE
Every change MUST result in 0 errors, 0 warnings, 0 regressions.

## 📖 READ COMPANION FILES FIRST
- `RULES-VERIFICATION.md` - Error prevention protocols
- `RULES-SVELTE5.md` - Svelte 5 patterns (MANDATORY)
- `RULES-TAILWIND-RESPONSIVE.md` - Tailwind v4 & breakpoints
- `RULES-SSR-HYDRATION-SEO.md` - SSR, CLS, Core Web Vitals
- `RULES-BACKEND.md` - Rust/Axum patterns

## 📦 Stack
- SvelteKit 2.50.1 + Svelte 5.48.2
- Tailwind CSS v4.1.18
- Rust/Axum 0.7 + PostgreSQL

## 🎯 BEFORE ANY CHANGE
1. READ entire file
2. TRACE imports/exports
3. RUN `npm run check` (must pass)
4. MAP dependencies

## ✅ AFTER EVERY CHANGE
```bash
npm run check    # 0 errors
npm run lint     # 0 warnings
npm run build    # success
npm run test     # all pass
```

## ⚡ SVELTE 5 ONLY
```svelte
let count = $state(0);
let doubled = $derived(count * 2);
let { title, onClose }: Props = $props();
$effect(() => { /* ... */ });
{@render children()}
<button onclick={handler}>
```

## ❌ BANNED (Svelte 4)
```
export let prop        → $props()
$: derived = x         → $derived()
<slot />               → {@render}
on:click               → onclick
createEventDispatcher  → callback props
```

## 🚨 MANTRA
"Zero errors. Trace dependencies. Update all affected files. Verify with evidence."