/**
 * Badge Component Types — PE7 (no Tailwind, no cn())
 * Variants handled via data-variant attribute + scoped CSS
 */
export type BadgeVariant = 'default' | 'secondary' | 'destructive' | 'outline';
