import Root from './button.svelte';
import {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
} from './button.types';

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
};
