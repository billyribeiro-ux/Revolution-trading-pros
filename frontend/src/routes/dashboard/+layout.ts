/**
 * Dashboard Layout Configuration
 * Disable prerendering for all authenticated dashboard routes
 *
 * @version 1.0.0
 * @author Revolution Trading Pros
 */

export const prerender = false;
export const ssr = true;
