/**
 * Template Selector Admin Page Server Configuration
 *
 * Disable prerendering since this page uses browser-only APIs
 * for template management and live preview.
 */

export const prerender = false;
export const ssr = false;
