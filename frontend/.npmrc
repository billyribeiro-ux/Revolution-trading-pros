# Revolution Trading Pros — npm/pnpm configuration
# (Audited 2026-07-19: stale justifications removed. Node is pinned to the
# 24.x LTS line via engines/.nvmrc; peer rules live in pnpm-workspace.yaml.)

# Ensure the public registry is used explicitly.
registry=https://registry.npmjs.org/
