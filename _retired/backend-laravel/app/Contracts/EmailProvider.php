<?php

declare(strict_types=1);

namespace App\Contracts;

/**
 * Email Provider Interface
 *
 * Contract for email provider implementations.
 * Intentionally minimal to support model implementations with attribute accessors.
 */
interface EmailProvider
{
    // Marker interface - methods are implemented as Eloquent attribute accessors
}
