name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  NODE_VERSION: "22"
  PNPM_VERSION: "10.33.2"
  RUST_VERSION: "stable"

jobs:
  # ───────────────────────────────────────────────────────────────────────
  # Frontend: typecheck + unit tests + build
  # ───────────────────────────────────────────────────────────────────────
  frontend:
    name: Frontend (check + vitest + build)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Typecheck (svelte-kit sync && svelte-check)
        run: pnpm --filter revolution-svelte check

      - name: Unit tests (vitest)
        run: pnpm --filter revolution-svelte test:unit

      - name: Production build
        run: pnpm --filter revolution-svelte build
        env:
          API_BASE_URL: <your-api-host>
          BACKEND_URL: <your-api-host>

  # ───────────────────────────────────────────────────────────────────────
  # Frontend: end-to-end (Playwright, chromium only — fastest meaningful gate)
  # ───────────────────────────────────────────────────────────────────────
  e2e:
    name: Frontend e2e (Playwright chromium)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Cache Playwright browsers
        id: pw-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-pw-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install Playwright browsers (chromium only)
        if: steps.pw-cache.outputs.cache-hit != 'true'
        working-directory: frontend
        run: pnpm exec playwright install --with-deps chromium

      - name: Run e2e
        working-directory: frontend
        run: pnpm exec playwright test tests/e2e --project=chromium --reporter=line

      - name: Upload e2e artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/test-results/
          retention-days: 7

  # ───────────────────────────────────────────────────────────────────────
  # Backend: cargo fmt + check + no-DB unit tests
  # ───────────────────────────────────────────────────────────────────────
  backend:
    name: Backend (cargo fmt + check + no-DB tests)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: api

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: api

      - name: cargo fmt --check
        run: cargo fmt --all --check

      - name: cargo clippy (warnings tolerated for now)
        run: cargo clippy --all-targets --all-features
        continue-on-error: true

      - name: cargo check
        run: cargo check --all-targets --all-features
        env:
          SQLX_OFFLINE: "true"

      - name: cargo test (no-DB unit tests)
        run: cargo test --test utils_test --test stripe_test
        env:
          SQLX_OFFLINE: "true"
